import { IPluginConfig } from 'kusu-plugin-sdk';

const inputConfig: IPluginConfig = {
  pluginName: 'input',
  pluginType: 'container',
  attributes: [],
  styles: [],
};

export default inputConfig;