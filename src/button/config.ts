import { IPluginConfig } from 'kusu-plugin-sdk';

const buttonConfig: IPluginConfig = {
  pluginName: 'button',
  pluginType: 'container',
  attributes: [],
  styles: []
};

export default buttonConfig;