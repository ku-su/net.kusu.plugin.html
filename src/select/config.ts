import { IPluginConfig } from 'kusu-plugin-sdk';

const selectConfig: IPluginConfig = {
  pluginName: 'select',
  pluginType: 'container',
  attributes: [],
  styles: []
};

export default selectConfig;